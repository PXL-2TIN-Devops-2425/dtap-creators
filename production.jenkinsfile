pipeline {
    agent any
    stages {
       stage('Cleanup') {
            steps {
                cleanWs()
            }
        }

        stage('deploy prod') {
            steps {
                sshagent(['prodserver']) {
                    sh "ssh -o StrictHostKeyChecking=no ubuntu@54.147.161.138 lieve91/calculatorapp:latest"
                }
            }
        }
    }
}
